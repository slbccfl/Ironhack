// app.js

var dymitri = {
	name:"Dymitri",
	noise:"Huff...",
	makeNoise: function() {
		console.log( this.name + 'says: ' + this.noise);
	},
	shout: function() {
		console.log (this.name + 'shouts: ' + this.noise.toUpperCase());
	}
};

	dymtri.makeNoise();
	dymiti.shout();

	