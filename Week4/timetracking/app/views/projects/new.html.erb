<h1> Projects </h1>

<h2>Enter a new project</h2>

<%= form_for @project do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :description %>
	<%= f.text_area :description %>

	<%= f.button %>
<% end %>